
import json
import os

EXTRA_DATA = {
    # ... (omitted for brevity, assume full data is here or I'll just use the existing one but fix it)
}
# I will actually edit the file generate_all_dossiers.py instead of rewriting it from scratch to avoid losing data.
